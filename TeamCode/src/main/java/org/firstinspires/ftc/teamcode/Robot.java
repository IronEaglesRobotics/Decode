package org.firstinspires.ftc.teamcode;import com.bylazar.configurables.annotations.Configurable;import com.pedropathing.follower.Follower;import com.qualcomm.hardware.rev.RevBlinkinLedDriver;import com.qualcomm.robotcore.hardware.ColorRangeSensor;import com.qualcomm.robotcore.hardware.DcMotorEx;import com.qualcomm.robotcore.hardware.DistanceSensor;import com.qualcomm.robotcore.hardware.HardwareMap;import com.qualcomm.robotcore.hardware.Servo;import com.seattlesolvers.solverslib.gamepad.GamepadEx;import com.seattlesolvers.solverslib.gamepad.GamepadKeys;import com.seattlesolvers.solverslib.hardware.motors.CRServoEx;import com.seattlesolvers.solverslib.hardware.motors.Motor;import com.seattlesolvers.solverslib.hardware.motors.MotorEx;import com.seattlesolvers.solverslib.hardware.servos.ServoEx;import org.firstinspires.ftc.robotcore.external.navigation.DistanceUnit;import org.firstinspires.ftc.teamcode.pedroPathing.Constants;import lombok.Getter;@Configurablepublic class Robot {    @Getter    public Lever lever;    @Getter    public Conveyor conveyor;    @Getter    private Follower follower;    @Getter    public Intake intake;    @Getter    public Shooter shooter;    @Getter    public LED ledController;    public Robot init(HardwareMap hardwareMap) {        this.lever = new Lever().init(hardwareMap);        this.conveyor = new Conveyor().init(hardwareMap);        this.intake = new Intake().init(hardwareMap);        this.shooter = new Shooter().init(hardwareMap);        this.follower = Constants.createFollower(hardwareMap);        this.ledController = new LED().init(hardwareMap);        return this;    }    @Configurable    public static class Lever {        private ServoEx lever;        public static double leverUp = .35;        public static double leverDown = 0.535;        public static double leverHold = 0.45;        public Lever init(HardwareMap hardwareMap) {            this.lever = new ServoEx(hardwareMap, "lever"); // T            return this;        }        public void up() {            lever.set(leverUp);        }        public void hold() {            lever.set(leverHold);        }        public void down() {            lever.set(leverDown);        }    }    @Configurable    public static class Conveyor {        private CRServoEx L1;        private CRServoEx L2;        private CRServoEx R1;        private CRServoEx R2;        private ServoEx Blocker;        public static double block = .1;        public static double unblock = .6;        public Conveyor init(HardwareMap hardwareMap) {            this.L1 = new CRServoEx(hardwareMap, "L1"); // This is the correct constructor!            this.L2 = new CRServoEx(hardwareMap, "L2");            this.R1 = new CRServoEx(hardwareMap, "R1");            this.R2 = new CRServoEx(hardwareMap, "R2");            this.Blocker = new ServoEx(hardwareMap, "blocker");            this.R1.setInverted(true);            this.L2.setInverted(true);            this.R2.setInverted(false);            return this;        }        private void setT1(Double power) {            L1.set(power);            R1.set(power);        }        private void setT2(Double power) {            L2.set(power);            R2.set(power);        }        public void block() {            Blocker.set(block);        }        public void unblock() {            Blocker.set(unblock);        }        public void stop() {            setT2(0.0);            setT1(0.0);        }        public void spit() {            setT1(-1.0);            setT2(-1.0);        }        public void intake() {            setT1(1.0);            setT2(1.0);        }        public void seperate() {            setT1(1.0);            setT2(-1.0);        }    }    @Configurable    public static class Intake {        private ColorRangeSensor iColor;        private DistanceSensor iDistance;        public static double distance = 4;        private DcMotorEx intake;        private static int in = -1;        private static double slow = -.3;        private static int out = 1;        public Intake init(HardwareMap hardwareMap) {            this.intake = hardwareMap.get(DcMotorEx.class, "intake");            this.iColor = hardwareMap.get(ColorRangeSensor.class, "iColor");            this.iDistance = hardwareMap.get(DistanceSensor.class, "iDistance");            this.iColor.enableLed(true);            return this;        }        public void setPower(double power) {            intake.setPower(power);        }        public void intake() {            setPower(in);        }        public void outtake() {            setPower(out);        }        public void slow() {            setPower(slow);        }        public double getDistance() {            return iColor.getDistance(DistanceUnit.CM);        }        public boolean hasBall() {            return getDistance() < distance;        }    }    @Configurable    public static class Shooter {        private ColorRangeSensor s1;        private ColorRangeSensor s2;        private MotorEx shooter;        private ServoEx shooterHood;        public static double powerFar = .63;        public static double powerNear = .5;        //        public static double powerNear = .45;        public static double hoodFar = .49;        public static double hoodNear = .54;        public static double rest = .67;        public int targetVelocity;        public static double distance = 4;        public static double threeBallDistance = 5;        public static double P = 69;        public static double I = 10;        public static double D = 0;        public Shooter init(HardwareMap hardwareMap) {            this.shooter = new MotorEx(hardwareMap, "motor", MotorEx.GoBILDA.BARE);            this.shooter.setRunMode(MotorEx.RunMode.VelocityControl);            this.shooter.setVeloCoefficients(P, I, D);            this.shooter.setPositionTolerance(1);            this.shooter.setCachingTolerance(0.0001);            this.shooter.setZeroPowerBehavior(Motor.ZeroPowerBehavior.FLOAT);            this.shooterHood = new ServoEx(hardwareMap, "shooterHood");            this.s1 = hardwareMap.get(ColorRangeSensor.class, "s1");            this.s2 = hardwareMap.get(ColorRangeSensor.class, "s2");            this.s1.enableLed(true);            this.s2.enableLed(true);            updateTargetVelocity(rest);            return this;        }        public double[] getPIDValues() {            return shooter.getVeloCoefficients();        }        public boolean atTargetVelocity() {            return (calculatedVelocity() >= targetVelocity - 50 && calculatedVelocity() <= targetVelocity + 80) || calculatedVelocity() > 4500;        }        private void updateTargetVelocity(double power) {            targetVelocity = (int) (power * 5333);        }        public int calculatedVelocity() {            return (int) shooter.getCorrectedVelocity() / 28 * 60;        }        public void setPower(double power) {            shooter.set(power);            updateTargetVelocity(power);        }        public void farShot() {            setPower(powerFar);            shooterHood.set(hoodFar);        }        public void nearShot() {            setPower(powerNear);            shooterHood.set(hoodNear);        }        public void rest() {            shooter.stopMotor();            shooterHood.set(rest);        }        public double getVelocity() {            return shooter.getVelocity();        }        public double[] getDistance() {            return new double[]{s1.getDistance(DistanceUnit.CM), s2.getDistance(DistanceUnit.CM)};        }        public boolean hasBall() {//            double totalDistance = 0;//            for (int i = 0; i < 3; i++) {//                totalDistance += (getDistance()[0] + getDistance()[1]) / 2;//            }//            lastChecked = totalDistance/5;            return (getDistance()[0] < distance || getDistance()[1] < distance);        }        public boolean has3Ball() {//            double totalDistance = 0;//            for (int i = 0; i < 3; i++) {//                totalDistance += (getDistance()[0] + getDistance()[1]) / 2;//            }//            lastChecked = totalDistance/5;            return (getDistance()[0] < distance || getDistance()[1] < threeBallDistance);        }        public void setNear(double power) {            powerNear = power;        }        public void setHood(double angle) {            hoodFar = angle;        }    }    @Configurable    public static class LED {        private RevBlinkinLedDriver ledDriver;        private Servo indicator;        public static double indicatorReady = 0.475;        public static double indicatorNotReady = 0.28;        public LED init(HardwareMap hardwareMap) {            this.ledDriver = hardwareMap.get(RevBlinkinLedDriver.class, "led");            this.indicator = hardwareMap.get(Servo.class, "indicator");            return this;        }        public void pattern0() {            ledDriver.setPattern(RevBlinkinLedDriver.BlinkinPattern.WHITE);        }        public void pattern1() {            ledDriver.setPattern(RevBlinkinLedDriver.BlinkinPattern.BLUE_GREEN);        }        public void pattern2() {            ledDriver.setPattern(RevBlinkinLedDriver.BlinkinPattern.BLUE_VIOLET);        }        public void pattern3() {            ledDriver.setPattern(RevBlinkinLedDriver.BlinkinPattern.STROBE_GOLD);        }        public void readyColor() {            indicator.setPosition(indicatorReady);        }        public void notReadyColor() {            indicator.setPosition(indicatorNotReady);        }    }    public void updateShooterStatus() {        if (getShooter().atTargetVelocity()) {            getLedController().readyColor();        } else {            getLedController().notReadyColor();        }    }    public robotStates robotstate = robotStates.CALIBRATE;    public enum robotStates {        CALIBRATE, IDLE, INTAKE, HAS1, HAS2, HAS3, SPIT, TRANSITION    }    public int transition = 0;    public int shootState = 0;    private boolean single = true;    public double delay;    public boolean D1 = false;    public int balls = 0;    public boolean rapid = false;    public static double foo = .15;    public static double leverTime = .35;    public boolean zit = false;    public boolean autoSwitch = false;    public void robotMacro(GamepadEx controller1, double runtime, boolean auto) {        updateShooterStatus();        boolean INTAKE = controller1.wasJustPressed(GamepadKeys.Button.DPAD_DOWN); ////        boolean rapidShoot = controller1.wasJustPressed(GamepadKeys.Button.A) || autoSwitch; //BUCKETH//        boolean shoot = controller1.wasJustPressed(GamepadKeys.Button.X); // BUCKETL//        boolean A = controller1.wasJustPressed(GamepadKeys.Button.A); // SPECIMENINTAKE//        boolean D2 = controller1.wasJustPressed(GamepadKeys.Button.DPAD_LEFT); // SPECIMENL//        boolean L1 = controller1.wasJustPressed(GamepadKeys.Button.LEFT_BUMPER); // RETRACT//        boolean L2 = controller1.getTrigger(GamepadKeys.Trigger.LEFT_TRIGGER) > .3;        boolean delayed = runtime > delay;        switch (robotstate) {            case CALIBRATE:                if (!zit) {                    delay = runtime + 2;                    zit = true;                }                conveyor.spit();                if (delayed) {                    robotstate = robotStates.IDLE;                }                break;            case IDLE:                //Idle Actions                conveyor.stop();                intake.setPower(0);                lever.down();                if (INTAKE) {                    robotstate = robotStates.INTAKE;                    balls = 0;                }                break;            case INTAKE:                conveyor.intake();                conveyor.block();                intake.intake();                lever.down();                ledController.pattern0();                //Intakes ball?                if (shooter.hasBall()) {                    robotstate = robotStates.HAS1;                    balls = 1;                }                break;            case HAS1:                conveyor.intake();                intake.intake();                lever.down();                ledController.pattern1();                //Intakes 2nd ball?                if (delayed) {                    if (shooter.hasBall() && intake.hasBall()) {                        robotstate = robotStates.HAS2;                        balls = 2;                        delay = runtime + .3;                    }                }                //Shoot?                shootCheck(controller1, runtime);                break;            case HAS2:                conveyor.intake();                intake.intake();                ledController.pattern2();                //Intakes 3rd ball?                if (delayed) {                    if (ballCheck(runtime, 0, .3)) {                        robotstate = robotStates.HAS3;                        balls = 3;                        zit = false;                        delay = runtime + .3;                    }                }                //Shoot?                shootCheck(controller1, runtime);                break;            case HAS3:                conveyor.intake();                intake.slow();                ledController.pattern3();//                if (!auto) {//                    if (!zit && delayed) {//                        if (!shooter.has3Ball() || !intake.hasBall()) {//                            robotstate = robotStates.HAS2;//                            balls = 2;//                        }//                        zit = true;//                    }//                }                //Shoot?                shootCheck(controller1, runtime);                break;            case TRANSITION:                if (delayed) {                    lever.down();                    conveyor.block();                    //Shot the ball?                    switch (transition) {                        //reload                        case 0:                            if (!ballCheck(runtime, 1, .1)) {                                intake.intake();                                conveyor.intake();                                transition++;                            }                            break;                        //done reloading?                        case 1:                            if (ballCheck(runtime, 1, foo) || balls == 1) {                                switch (balls) {                                    case 1:                                        robotstate = robotStates.INTAKE;                                        rapid = false;                                        break;                                    case 2:                                        robotstate = robotStates.HAS1;//                                        lever.down();                                        break;                                    case 3:                                        robotstate = robotStates.HAS2;//                                        lever.down();                                        break;                                }                                balls--;                                transition = 0;                                delay = runtime + .3;                            }                            break;                    }                }                break;            case SPIT:                break;        }    }    private boolean ballCheck(double runtime, int tier, double checktime) {        if (tier == 1) {            if (shooter.hasBall() && !D1) {                D1 = true;                delay = runtime + checktime;            } else if (shooter.hasBall() && D1 && runtime > delay) {                D1 = false;                return true;            }        } else {            if (intake.hasBall() && !D1) {                D1 = true;                delay = runtime + checktime;            } else if (intake.hasBall() && D1 && runtime > delay) {                D1 = false;                return true;            }        }        return false;    }    private void shootCheck(GamepadEx controller1, double runtime) {        boolean rapidShoot = controller1.wasJustPressed(GamepadKeys.Button.A) || autoSwitch; //BUCKETH        boolean shoot = controller1.wasJustPressed(GamepadKeys.Button.X); // BUCKETL        boolean delayed = runtime > delay;        switch (shootState) {            case 0:                if (shoot) {                    conveyor.unblock();                    single = true;                    delay = runtime + .1;                    shootState++;                } else if ((rapid || rapidShoot) && getShooter().atTargetVelocity()) {                    conveyor.unblock();                    single = false;                    rapid = true;                    delay = runtime + .1;                    shootState++;                }                break;            case 1:                if (delayed) {                    robotstate = robotStates.TRANSITION;                    lever.up();                    delay = runtime + leverTime;                    shootState = 0;                    break;                }        }    }}